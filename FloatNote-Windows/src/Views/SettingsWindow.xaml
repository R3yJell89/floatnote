<Window x:Class="FloatNote.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Name="SettingsRoot"
        Title="Настройки FloatNote"
        Height="580" Width="440"
        MinHeight="500" MinWidth="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        Loaded="SettingsWindow_Loaded">

    <Border Background="{StaticResource AcrylicBackgroundBrush}"
            BorderBrush="{StaticResource SubtleBorderBrush}"
            BorderThickness="1"
            CornerRadius="10">

        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/> <!-- Header -->
                <RowDefinition Height="*"/>  <!-- Settings Cards -->
                <RowDefinition Height="42"/> <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- Header -->
            <Grid Grid.Row="0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="⚙" FontSize="16" Foreground="{StaticResource AccentGreenBrush}" VerticalAlignment="Center"/>
                    <TextBlock x:Name="SettingsTitleText"
                               Text="Настройки FloatNote"
                               FontFamily="{StaticResource SegoeUIVariable}"
                               FontWeight="SemiBold"
                               FontSize="14"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               Margin="8,0,0,0"
                               VerticalAlignment="Center"/>
                </StackPanel>
                <Button Click="Close_Click" HorizontalAlignment="Right" Style="{StaticResource WindowCaptionButtonStyle}" Content="✕"/>
            </Grid>

            <!-- Settings Content (Cards) -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="0,8">
                <StackPanel>

                    <!-- Card 0: Language Settings -->
                    <Border Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock x:Name="LangCardTitle" Text="Языковые настройки" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,6"/>

                            <!-- Interface language -->
                            <TextBlock x:Name="LangInterfaceLabel" Text="Язык интерфейса:" Foreground="{StaticResource TextPrimaryBrush}" FontSize="11" Margin="0,0,0,4"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <RadioButton x:Name="RadioLangRU" Content="RU — Русский" Style="{StaticResource PillTabButtonStyle}" Margin="0,0,6,0" GroupName="AppLang"/>
                                <RadioButton x:Name="RadioLangEN" Content="EN — English"  Style="{StaticResource PillTabButtonStyle}" GroupName="AppLang"/>
                            </StackPanel>

                            <!-- Dictation language -->
                            <TextBlock x:Name="LangDictLabel" Text="Язык диктовки:" Foreground="{StaticResource TextPrimaryBrush}" FontSize="11" Margin="0,0,0,4"/>
                            <StackPanel Orientation="Horizontal">
                                <RadioButton x:Name="RadioDictRU" Content="ru-RU" Style="{StaticResource PillTabButtonStyle}" Margin="0,0,6,0" GroupName="DictLang"/>
                                <RadioButton x:Name="RadioDictEN" Content="en-US" Style="{StaticResource PillTabButtonStyle}" GroupName="DictLang"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <!-- Card 1: NLE Selection -->
                    <Border Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock x:Name="NLECardTitle" Text="Основной видеоредактор (NLE)" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,8">
                                <RadioButton x:Name="RadioDVR" Content="DaVinci Resolve" IsChecked="True" Style="{StaticResource PillTabButtonStyle}" Margin="0,0,6,0" GroupName="NLE"/>
                                <RadioButton x:Name="RadioPR"  Content="Premiere Pro"    Style="{StaticResource PillTabButtonStyle}" GroupName="NLE"/>
                            </StackPanel>
                            <CheckBox x:Name="CheckAutoMarkers"
                                      IsChecked="True"
                                      Foreground="{StaticResource TextPrimaryBrush}"
                                      FontSize="12"
                                      Margin="0,4,0,0">
                                <TextBlock x:Name="AutoMarkersLabel" Text="Автосоздание маркера в DaVinci при добавлении задачи" TextWrapping="Wrap"/>
                            </CheckBox>
                        </StackPanel>
                    </Border>

                    <!-- Card 2: Window Behavior -->
                    <Border Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,10">
                        <StackPanel>
                            <TextBlock x:Name="WindowCardTitle" Text="Поведение окна" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                            <CheckBox x:Name="CheckAlwaysOnTop"
                                      IsChecked="True"
                                      Foreground="{StaticResource TextPrimaryBrush}"
                                      FontSize="12"
                                      Margin="0,8,0,0">
                                <TextBlock x:Name="AlwaysOnTopLabel" Text="Закреплять окно поверх всех окон (Always on Top)" TextWrapping="Wrap"/>
                            </CheckBox>
                        </StackPanel>
                    </Border>

                    <!-- Card 3: Global Hotkeys (PowerToys Style) -->
                    <Border Background="{StaticResource CardBackgroundBrush}" BorderBrush="{StaticResource SubtleBorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12">
                        <StackPanel>
                            <TextBlock x:Name="HotkeysCardTitle" Text="Глобальные горячие клавиши" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,0,0,8"/>

                            <!-- Hotkey 1: Toggle Window -->
                            <TextBlock x:Name="HkWinLabel" Text="Показать / скрыть окно:" Foreground="{StaticResource TextPrimaryBrush}" FontSize="11" Margin="0,0,0,4"/>
                            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                <ToggleButton x:Name="HkWinCtrl"  Content="Ctrl"  Width="40" Height="26" Margin="0,0,4,0" Background="#20FFFFFF" Foreground="White" BorderThickness="0"/>
                                <ToggleButton x:Name="HkWinAlt"   Content="Alt"   Width="40" Height="26" Margin="0,0,4,0" Background="#20FFFFFF" Foreground="White" BorderThickness="0"/>
                                <ToggleButton x:Name="HkWinShift" Content="Shift" IsChecked="True" Width="44" Height="26" Margin="0,0,6,0" Background="{StaticResource AccentGreenBrush}" Foreground="White" BorderThickness="0"/>
                                <ComboBox x:Name="HkWinKey" Width="80" Height="26" SelectedIndex="1">
                                    <ComboBoxItem Content="1"/>
                                    <ComboBoxItem Content="2"/>
                                    <ComboBoxItem Content="Space"/>
                                    <ComboBoxItem Content="F1"/>
                                    <ComboBoxItem Content="F2"/>
                                </ComboBox>
                            </StackPanel>

                            <!-- Hotkey 2: Ghost Click-Through -->
                            <TextBlock x:Name="HkGhostLabel" Text="Сквозной клик (Ghost Mode):" Foreground="{StaticResource TextPrimaryBrush}" FontSize="11" Margin="0,0,0,4"/>
                            <StackPanel Orientation="Horizontal">
                                <ToggleButton x:Name="HkGhostCtrl"  Content="Ctrl"  IsChecked="True" Width="40" Height="26" Margin="0,0,4,0" Background="{StaticResource AccentGreenBrush}" Foreground="White" BorderThickness="0"/>
                                <ToggleButton x:Name="HkGhostAlt"   Content="Alt"   Width="40" Height="26" Margin="0,0,4,0" Background="#20FFFFFF" Foreground="White" BorderThickness="0"/>
                                <ToggleButton x:Name="HkGhostShift" Content="Shift" Width="44" Height="26" Margin="0,0,6,0" Background="#20FFFFFF" Foreground="White" BorderThickness="0"/>
                                <ComboBox x:Name="HkGhostKey" Width="80" Height="26" SelectedIndex="0">
                                    <ComboBoxItem Content="1"/>
                                    <ComboBoxItem Content="2"/>
                                    <ComboBoxItem Content="G"/>
                                    <ComboBoxItem Content="F3"/>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>

            <!-- Footer: Save & Close -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button x:Name="SaveButton" Click="Save_Click" Content="Сохранить" Background="{StaticResource AccentGreenBrush}" Foreground="White" Padding="16,6" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>
